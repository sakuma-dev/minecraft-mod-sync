# 開発の進め方

この文書をブランチ運用とPRの手順の正本とする。機能の範囲は[要件定義](docs/requirements.md)、実装時の確認事項は[実装・検証計画](docs/implementation-plan.md)を参照する。

## ブランチ構成

| ブランチ | 役割 | 変更の取り込み先 |
| --- | --- | --- |
| `main` | 検証済みの安定版を置く | `dev`からのリリースPR |
| `dev` | 開発内容の統合先。GitHubのデフォルト | 各作業ブランチからのPR |
| `feat/*` | 機能追加 | `dev` |
| `fix/*` | 不具合修正 | `dev` |
| `docs/*` | 文書の変更 | `dev` |
| `chore/*` | 開発環境やCI等の整備 | `dev` |

通常の作業ブランチは最新の`dev`から作る。`main`と`dev`は長期運用し、直接push、force-push、削除を行わない。リポジトリ作成と今回の運用開始用文書の登録だけは、初期セットアップとして直接コミットする。

現在は文書と開発用の検査・CIを整備済みで、アプリ本体は未実装。`main`を安定版の置き場とすることは、現在のコミットが配布済みアプリであるという意味ではない。

## 通常の開発

1. 最新の`dev`を取得し、変更内容に合った作業ブランチを作る。
2. 要件や動作を変える場合は、関係する仕様と受入条件を先に整理する。
3. 変更内容に応じた検証を実行する。
4. 作業ブランチをpushし、`dev`向けのPRを作る。
5. 差分と検証結果をレビューしてからマージする。

例としてIssue 123の機能を追加する場合：

```powershell
git switch dev
git pull --ff-only origin dev
git switch -c feat/issue-123
```

PR本文には、変更の目的・主な動作・検証結果・残る確認事項を書く。関連Issueがあればリンクする。

## 安定版への反映

1. `dev`上のリリース候補について、必要な自動検証とWindows実機確認を完了する。
2. バージョンやリリース説明の更新も作業ブランチから`dev`へ取り込む。取り込み後の候補を検証する。
3. `dev`から`main`へリリースPRを作り、確認後に通常のmerge（Create a merge commit）で取り込む。
4. リリースを配布する際は、検証した`main`のコミットにタグを付ける。配布物を確認して公開する。
5. `main`から`dev`への同期PRも通常のmergeで取り込み、次の開発へ進む。

長期ブランチ同士をsquash mergeしない。公開済みタグを移動しない。具体的なバージョン管理と梱包・公開手順は、アプリの実装・配布方式が決まった段階で定める。

過去版の保守が必要になるまでは、`release/*`を先に作らない。必要になった時に対象タグ、修正の取り込み先、検証方法を決める。

## 現在の検証とGitHub設定

現在のCIは[Repository CI](.github/workflows/ci.yml)の`Repository checks`。Windows上で検査スクリプトのテスト、追跡Markdownの厳格UTF-8・改行・ローカルリンク、JSONの構文、`git diff --check`を確認する。`main`・`dev`・`release/**`向けPRとpushで実行し、パス条件による省略をしない。アプリのビルド・テストが実行できたことにはしない。

GitHubのデフォルトブランチは`dev`とする。[保護ルールの設定ファイル](.github/rulesets/branch-protection.json)は`main`・`dev`・`release/`配下を対象にし、PR、会話の解決、最新の取り込み先を含む`Repository checks`の成功を要求する。GitHub Actions（App ID 15368）からのチェックだけを認め、削除とforce-pushを禁止する。管理者の常用bypassは設けない。

現在の共同開発者は所有者本人のみのため、承認人数は0とし、PRとCIを必須にしている。参照元の承認1人は、そのまま設定すると本人のPRを本人が承認できず止まるため採用しない。別のレビュアーが参加した段階で承認条件を見直す。対象ブランチの作成制限は加えず、新規作成時だけCIの必須条件を適用しない。これにより過去タグから保守ブランチを作成でき、作成後の更新にはPRと必須CIを課す。

2026-09-09に所有者の指示でリポジトリを公開し、[branch-protectionルール](https://github.com/sakuma-dev/minecraft-mod-sync/rules/22642836)を有効化した。GitHub APIで設定ファイルとの一致と、`main`・`dev`・`release/v0.1`に対するルールの適用を確認した。`Repository checks`はGitHub Actionsで成功を確認してから必須化している。設定ファイルを変更しただけではGitHub側のルールは変わらないため、今後の変更でもAPIまたは設定画面で適用し、読み戻して確認する。

実装基盤を導入した段階でアプリのビルド・テストをCIへ追加し、実際に成功するチェックを確認してから必須化する。存在しないチェック名を先に登録しない。CIがない過去タグから保守ブランチを作る場合は、保守用のCIを含むPRで準備する。

## 参考

[Villager Translatorの開発手順](https://github.com/yoshizawa0202/villager-translator/blob/main/CONTRIBUTING.md)の`main`・`dev`の役割、PRの流れ、長期ブランチのmerge方針を参考にした。2026-09-09にGitHub APIで確認した参考元のデフォルトブランチは`main`だったが、このプロジェクトでは`dev`を使う。参考元のFlutter向けコマンドやCI設定は、このプロジェクトへそのまま適用しない。
