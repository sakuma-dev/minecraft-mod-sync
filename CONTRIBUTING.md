# 開発の進め方

この文書をブランチ運用とPRの手順の正本とする。機能の範囲は[要件定義](docs/requirements.md)、実装時の確認事項は[実装・検証計画](docs/implementation-plan.md)を参照する。

2026-09-11に、`sakuma-dev`と`yoshizawa0202`が小さなIssueとPRで進捗を共有する運用を正式採用した。共有する作業内容・進捗・相談・確認結果はGitHubへ残し、この文書を運用ルールの正本とする。

## ブランチ構成

| ブランチ | 役割 | 変更の取り込み先 |
| --- | --- | --- |
| `main` | 検証済みの安定版を置く | `dev`からのリリースPR |
| `dev` | 開発内容の統合先。GitHubのデフォルト | 各作業ブランチからのPR |
| `feat/*` | 機能追加 | `dev` |
| `fix/*` | 不具合修正 | `dev` |
| `docs/*` | 文書の変更 | `dev` |
| `chore/*` | 開発環境やCI等の整備 | `dev` |

通常の作業ブランチは最新の`dev`から作る。`main`と`dev`は長期運用し、直接push、force-push、削除を行わない。リポジトリ作成と今回の運用開始用文書の登録だけは、初期セットアップとして直接コミットする。

現在は文書、開発基盤、起動確認用の最小WPFアプリを用意している。同期・Prism連携は未実装。[セットアップ手順](docs/development.md)で固定SDKの導入、ビルド・テスト・起動を確認する。`main`を安定版の置き場とすることは、現在のコミットが配布済みアプリであるという意味ではない。

## 通常の開発

1. 着手前にIssueを作り、目的・対象範囲・完了条件・実装担当者を書く。要件や動作を変える場合は、関係する仕様と受入条件も先に整理する。
2. 1つの小さなIssueを1人が担当する。もう1人は相談・レビュー・動作確認を担い、作業ごとに交代してよい。1 Issue・1作業ブランチ・1 PRを基本にし、半日〜1日程度で成果を確認できる大きさを目安とする。時間の約束ではなく、大きくなった場合は作業を分ける。
3. 最新の`dev`から、変更内容に合った作業ブランチを作る。同じブランチを2人で同時編集しない。並行作業は別Issue・別ブランチで進め、重なるファイルや前提となる仕様があれば先に調整する。
4. 最初のまとまった変更をコミット・pushした時点で、`dev`向けのDraft PRを作り、Issueへリンクする。完成前から方針と差分を共有する。
5. 意味のある区切りごとに日本語のメッセージで小さくコミットし、pushして進捗を更新する。変更内容に応じた検証も行い、結果と未確認の範囲を記録する。
6. Issueの完了条件を満たしたらPRをReady for reviewへ変更する。CI成功と差分・検証結果を確認して`dev`へマージし、完了したIssueを閉じる。Draftを解除しただけで完了扱いにしない。

例えばIssue 123の機能追加では、作業ブランチ名を`feat/issue-123`とする。文書なら`docs/issue-123`、不具合修正なら`fix/issue-123`を使う。

```powershell
git switch dev
git pull --ff-only origin dev
git switch -c feat/issue-123
```

PR本文には、対象Issue、変更の目的・主な動作・検証結果・残る確認事項を書く。実機確認が必要な完了条件を、自動テストやビルド成功だけで完了にしない。PRで解決しない作業は、未完了と分かる形でIssueに残す。

## 進捗共有と相談

共有するタイミングは、着手時・意味のある区切り・行き詰まった時・作業終了時とする。PR作成前はIssueに、作成後はそのPRに進捗をまとめる。ローカルでコミットしただけでは相手に届かないため、共有時はpushと短い説明を組み合わせる。

```text
できたこと：
確認できたこと：
未確認・困っていること：
次にすること：
相手に確認してほしいこと：
```

通常の小さな変更は、作業者が完了条件・CI・差分を確認し、相手の応答待ちだけを理由に止めず取り込める。配布形式・ファイル削除・復旧処理など、相手の作業や利用者データへ影響する変更は、着手前に方針と影響を相手へ共有して相談する。未解決の指摘がある場合は内容を解消してからマージする。

マージ後の作業ブランチはGitHubの自動削除設定で整理する。`main`・`dev`・保守中の`release/*`は維持する。次の作業は改めて最新の`dev`から始める。

## 安定版への反映

1. `dev`上のリリース候補について、必要な自動検証とWindows実機確認を完了する。
2. バージョンやリリース説明の更新も作業ブランチから`dev`へ取り込む。取り込み後の候補を検証する。
3. `dev`から`main`へリリースPRを作り、確認後に通常のmerge（Create a merge commit）で取り込む。
4. リリースを配布する際は、検証した`main`のコミットにタグを付ける。配布物を確認して公開する。
5. `main`から`dev`への同期PRも通常のmergeで取り込み、次の開発へ進む。

長期ブランチ同士をsquash mergeしない。公開済みタグを移動しない。具体的なバージョン管理と梱包・公開手順は、アプリの実装・配布方式が決まった段階で定める。

過去版の保守が必要になるまでは、`release/*`を先に作らない。必要になった時に対象タグ、修正の取り込み先、検証方法を決める。

## 現在の検証とGitHub設定

現在のCIは[Repository CI](.github/workflows/ci.yml)の`Repository checks`。Windows上で検査スクリプトのテスト、追跡Markdownの厳格UTF-8・改行・ローカルリンク、JSONの構文、`git diff --check`を確認する。固定SDKでのアプリビルド・Core/Platformのテスト・WPF起動の自動確認も同じ必須チェック内で行う。`main`・`dev`・`release/**`向けPRとpushで実行し、パス条件による省略をしない。CIの成功をMinecraftへの実参加や2人のPCでの確認の代用にしない。

GitHubのデフォルトブランチは`dev`とする。[保護ルールの設定ファイル](.github/rulesets/branch-protection.json)は`main`・`dev`・`release/`配下を対象にし、PR、会話の解決、最新の取り込み先を含む`Repository checks`の成功を要求する。GitHub Actions（App ID 15368）からのチェックだけを認め、削除とforce-pushを禁止する。管理者の常用bypassは設けない。

必要なレビュー承認人数は、2026-09-11のユーザーの明示指示により0人とする。共同開発者の有無にかかわらず、PRとCIは必須にし、通常変更を一律に相手の承認待ちにしない。重要な変更は前節の相談ルールで扱う。対象ブランチの作成制限は加えず、新規作成時だけCIの必須条件を適用しない。これにより過去タグから保守ブランチを作成でき、作成後の更新にはPRと必須CIを課す。

2026-09-09に所有者の指示でリポジトリを公開し、[branch-protectionルール](https://github.com/sakuma-dev/minecraft-mod-sync/rules/22642836)を有効化した。GitHub APIで設定ファイルとの一致と、`main`・`dev`・`release/v0.1`に対するルールの適用を確認した。`Repository checks`はGitHub Actionsで成功を確認してから必須化している。設定ファイルを変更しただけではGitHub側のルールは変わらないため、今後の変更でもAPIまたは設定画面で適用し、読み戻して確認する。

新しい必須チェックを分けて追加する場合は、実際に成功する実行を確認してから必須化する。存在しないチェック名を先に登録しない。CIがない過去タグから保守ブランチを作る場合は、保守用のCIを含むPRで準備する。

## 参考

[Villager Translatorの開発手順](https://github.com/yoshizawa0202/villager-translator/blob/main/CONTRIBUTING.md)の`main`・`dev`の役割、PRの流れ、長期ブランチのmerge方針を参考にした。2026-09-09にGitHub APIで確認した参考元のデフォルトブランチは`main`だったが、このプロジェクトでは`dev`を使う。参考元のFlutter向けコマンドやCI設定は、このプロジェクトへそのまま適用しない。
